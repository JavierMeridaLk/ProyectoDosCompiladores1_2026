/* 04-logic.comp */
statusComp(int $status) {
    if ($status == 1) {
        T("Iniciado")
    } else ($status == 2) {
        T("En progreso")
    } else {
        Switch($status) {
            case 3 { T("Finalizado") },
            default { T("Desconocido") }
        }
    }
}